# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
answer = 0
N, c = map(int, input().split())
l = list(map(int, input().split()))

for i in range(N):
	if l[i] - l[i-1] > c:
		answer = 0
	answer += 1

print(answer)